generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Teams {
  id     String @id
  name   String
  abbr   String
  market String
}

model Credentials {
  id       String @id
  email    String
  @@unique([email])
}
